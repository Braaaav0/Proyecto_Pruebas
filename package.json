{
  "name": "biblioteca-app",
  "version": "1.0.0",
  "description": "Sistema de gestión de biblioteca (login, menú, 5 maestras, préstamos y 2 reportes)",
  "type": "module",
  "private": true,
  "engines": { "node": ">=22.13" },
  "scripts": {
    "start": "node --disable-warning=ExperimentalWarning src/server.js",
    "seed:demo": "node --disable-warning=ExperimentalWarning src/seed-demo.js",
    "test": "node --disable-warning=ExperimentalWarning --test --test-reporter=spec",
    "test:coverage": "node --disable-warning=ExperimentalWarning --test --experimental-test-coverage --test-reporter=spec"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
